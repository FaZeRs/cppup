{
  "name": "{{name}}",
  "version": "{{version}}",
  "dependencies": [
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    },
    {{#if (eq test_framework "doctest")}}
    "doctest",
    {{/if}}
    {{#if (eq test_framework "gtest")}}
    "gtest"
    {{/if}}
    {{#if (eq test_framework "catch2")}}
    "catch2"
    {{/if}}
    {{#if (eq test_framework "boost")}}
    "boost"
    {{/if}}
  ]
}